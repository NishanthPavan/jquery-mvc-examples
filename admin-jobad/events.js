/**
 * jQuery allows event binding on objects.
 * This module creates an empty objects and wraps it with jquery,
 * allowing us to define global application events
 */

define(['jquery'], function($) {
    return $({});
});